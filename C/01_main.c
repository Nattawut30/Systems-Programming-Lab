// for mac complier: clang --version
// xcode-select --install for install Clang and dev tools

// C/C++ Extension pack on VsCode
// downlaod "Code Runner" on the extension

// Vscode -> Setting -> Code runner: "Check clear output before run" and "save file before run"

#include <stdio.h> // Preprocessor directive

int main(){

    // This is my fist program! << Use "// as a comment"

    /*
    This
    is
    also
    a 
    comment
    */
    
    printf("Hello, World!\n");
    printf("I like pizza!\n");
    printf("It's really good!\n");

    return 0; // serve as a exit code
}